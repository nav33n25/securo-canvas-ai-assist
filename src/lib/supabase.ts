
// Supabase client stub - Replace with actual implementation when ready
import { createClient } from '@supabase/supabase-js';

// Empty config for now to fix build errors
export const supabase = createClient(
  'https://placeholder-url.supabase.co', 
  'placeholder-api-key'
);
